from collections import defaultdict

hashmap.keys()
# returns all of the keys from the dict in the form a a list
    
hashmap.values()
# returns all of the values from the dict in the form a a list

hashmap.items()
# combination of the two, it returns a list of all of the key value pairs as tuples